# SQL Server SA Password (used by docker-compose to launch the container)
MSSQL_SA_PASSWORD=YourStrongPassword123!

# Database connection string for Prisma ORM (The password value MUST match MSSQL_SA_PASSWORD)
DATABASE_URL="sqlserver://localhost:1433;database=nursing_home_db;user=sa;password=YourStrongPassword123!;encrypt=true;trustServerCertificate=true;"
